import React from "react";

import "./Navbar.scss";

const Navbar = (props) => {
  return (
    <div className="Navbar">
      <nav>
        <p>
          word<span>.</span>counter
        </p>
      </nav>
    </div>
  );
};

export default Navbar;
